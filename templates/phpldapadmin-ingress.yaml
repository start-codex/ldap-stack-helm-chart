{{- if and .Values.phpldapadmin.enabled .Values.phpldapadmin.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ldap-stack.phpldapadmin.fullname" . }}
  labels:
    {{- include "ldap-stack.phpldapadmin.labels" . | nindent 4 }}
  {{- with .Values.phpldapadmin.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.phpldapadmin.ingress.className }}
  ingressClassName: {{ .Values.phpldapadmin.ingress.className }}
  {{- end }}
  {{- if .Values.phpldapadmin.ingress.tls }}
  tls:
    {{- range .Values.phpldapadmin.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.phpldapadmin.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "ldap-stack.phpldapadmin.fullname" $ }}
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
